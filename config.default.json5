{
	// Default capacity: 500 entries
	// Default hostname: 'localhost'
	// Default port: 8080
	apiUrl: '<INSERT API URL HERE>', // Replace this with your API URL
	cache: {
		GET: ['*'],
		HEAD: ['*'],
	},
	bust: {
		POST: {
			"*": [] // * must be enclosed in double quotes when used as a key because of a bug
		},
		PUT: {
			"*": [] // * must be enclosed in double quotes when used as a key because of a bug
		},
		DELETE: {
			"*": [] // * must be enclosed in double quotes when used as a key because of a bug
		},
	},
}
