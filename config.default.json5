{
	capacity: 500, 
	hostname: 'localhost', //delete
	port: 8080, // delete
	apiUrl: 'https://jsonplaceholder.typicode.com/', // Replace this with your API URL
	// apiUrl: '<INSERT API URL HERE>', // Replace this with your API URL
	cache: {
		GET: ['*'],
		HEAD: ['*'],
	},
	bust: {
		POST: {
			"*": [] // * must be enclosed in double quotes when used as a key because of a bug
		},
		PUT: {
			"*": [] // * must be enclosed in double quotes when used as a key because of a bug
		},
		DELETE: {
			"*": [] // * must be enclosed in double quotes when used as a key because of a bug
		},
	},
}
